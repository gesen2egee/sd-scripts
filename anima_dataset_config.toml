[general]
shuffle_caption = false
caption_extension = '.txt'

[[datasets]]
resolution = [640, 640]
batch_size = 1
enable_bucket = true
min_bucket_reso = 256
max_bucket_reso = 1024
bucket_reso_steps = 16
bucket_no_upscale = true

  [[datasets.subsets]]
  # Same source folder (pass 1 / 512 profile).
  image_dir = 'E:/NE/20_miss valentine'
  num_repeats = 1
  caption_extension = '.txt'

[[datasets]]
resolution = [768, 768]
batch_size = 1
enable_bucket = true
min_bucket_reso = 384
max_bucket_reso = 1152
bucket_reso_steps = 16
bucket_no_upscale = true

  [[datasets.subsets]]
  # Same source folder (pass 2 / 768 profile).
  image_dir = 'E:/NE/20_miss valentine'
  num_repeats = 1
  caption_extension = '.txt'

[[datasets]]
resolution = [1024, 1024]
batch_size = 1
enable_bucket = true
min_bucket_reso = 512
max_bucket_reso = 1536
bucket_reso_steps = 16
bucket_no_upscale = true

  [[datasets.subsets]]
  # Same source folder (pass 3 / 1024 profile).
  image_dir = 'E:/NE/20_miss valentine'
  num_repeats = 1
  caption_extension = '.txt'
